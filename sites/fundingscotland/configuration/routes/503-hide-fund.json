{
  "name": "hide-fund",
  "pattern": "/hide-fund",
  "acceptedVerbs": "POST",
  "debug": true,
  "tasks": [
    "context.metaData.commonTasks.currentSite",
    "context.metaData.commonTasks.authRequired",
    {
      "name": "notes",
      "taskModule": "firebaseRtbSet",
      "renderer": "handlebars",
      "config": {
        "appName": "fundingscotland",
        "pathTemplate": "/users/{{data.auth.uid}}/{{data.currentSite.name}}/hidden/{{request.body.id}}",
        "setOrUpdate": "set",
        "dataTemplate": true
      }
    },
    {
      "name": "destination",
      "taskModule": "render",
      "renderer": "handlebars",
      "config": {
        "output": "body",
        "template": "{ \"id\": {{{stringify request.body.id}}} }",
        "parseJson": true
      }
    }
  ]
}