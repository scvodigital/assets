{
  "name": "testpage",
  "pattern": "/testpage",
  "debug": true,
  "acceptedVerbs": "GET",
  "defaultParams": {
    "context": "results"
  },
  "tasks": [
    "context.metaData.commonTasks.authNotRequired",
    "context.metaData.commonTasks.getExternalProfileId",
    "context.metaData.commonTasks.externalProfileRequired",
    {
      "name": "destination",
      "taskModule": "renderLayout",
      "renderer": "handlebars",
      "config": {
        "logic": {
          "if": [
            "default"
          ]
        },
        "output": "body",
        "layouts": {
          "default": {
            "layout": ">context.metaData.handlebars.layouts.default",
            "parts": {
              "head": ">context.metaData.handlebars.layouts.pages_page-head-tags",
              "body": ">context.metaData.handlebars.templates.pages_test"
            },
            "contentType": "text/html"
          }
        }
      }
    }

  ]
}